{
  "name": "text-scramble-design-plugins",
  "version": "1.0.1",
  "private": true,
  "description": "Shape-matched placeholder copy for Figma and Adobe InDesign.",
  "author": "pitch.dog",
  "license": "MIT",
  "homepage": "https://github.com/bomkino/text-scramble-design-plugins#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bomkino/text-scramble-design-plugins.git"
  },
  "bugs": {
    "url": "https://github.com/bomkino/text-scramble-design-plugins/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm test && npm run build && node scripts/verify-build.mjs",
    "package": "npm run verify && node scripts/package.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.131.0",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
